<!DOCTYPE html>
<html>

<head>
    <title>RailsWithJeremy</title>
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>

    <%= stylesheet_link_tag 'application', media: 'all', 'data-turbolinks-track': 'reload' %>
    <%= javascript_pack_tag 'application', 'data-turbolinks-track': 'reload' %>
</head>

<body>

    <% if current_user %>
    <h1>Current User: <%= current_user.name %></h1>
    <%# QUESTION: I still get confused by the logout_path names  %>
    <%= button_to  "log out", logout_path, method: :delete %>
    <hr />
    <% end %>


    <% flash.each do |type, msg| %>
    <div>
        <%= msg %>
    </div>

    <% end %>

    <%= yield %>
</body>

</html>